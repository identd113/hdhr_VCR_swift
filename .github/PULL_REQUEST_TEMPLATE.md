## What does this change?

## Why?

## Testing

- [ ] `swift build` passes
- [ ] `swift test` passes (full Xcode required — see `CLAUDE.md`)
- [ ] Manually verified the change in the running app, if it's UI/behavior-visible

## Checklist

- [ ] Updated `docs/*.md` if this changes documented behavior (see `CLAUDE.md`'s "Documentation" section)
- [ ] Added a `CHANGELOG.md` entry if this is a user-visible fix or feature
